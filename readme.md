把ZIP裡面的所有壓縮檔都解壓縮到同一個目錄

需要的dataset和其他data由於檔案太大，我存放在EE603-2 C-2 的E槽裡面 "E:\交接TTP detection\ZIP\Data_20260818_1433.zip" (壓縮後29GB)，一樣把他跟上述ZIP解壓縮在同一個目錄下。

Code含模型的使用方式在壓縮檔內的README.md

### 由於原環境是GB10 Spark 的ARM架構，如要在Windows上跑需自行調整code寫法和套件版本。
